# Deterministic demo scenario

A Pi session has two extensions registering the same command and one package importing the deprecated Mario Zechner namespace. Run `/extension-doctor` and capture the bounded local report at 80×24 and 120×36. No network, credentials, or package mutation is involved.
